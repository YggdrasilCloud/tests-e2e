# E2E Test Environment Configuration
# This file is used by the CI/CD pipeline to specify which versions to test

# Core API version
# Can be: tag (v1.2.3), branch (main), or commit SHA
CORE_VERSION=main

# Frontend version
# Can be: tag (v1.2.3), branch (main), or commit SHA
FRONTEND_VERSION=main
