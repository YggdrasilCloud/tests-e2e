# Docker image versions (pas utilisé, on build localement)
BACKEND_VERSION=latest
FRONTEND_VERSION=latest

# Playwright configuration
# Ports différents du dev pour éviter les conflits :
# - DB test: 5433 (dev: 5432)
# - Backend test: 8001 (dev: 8000)
# - Frontend test: 5174 (dev: 5173)
BASE_URL=http://localhost:5174
CI=false
